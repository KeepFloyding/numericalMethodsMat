function a = propensityFunction(X)

%==================================================================
% GENERATING PROPENSITY FUNCTION
%==================================================================

a = zeros(1, length(X));

end